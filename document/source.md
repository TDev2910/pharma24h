Bạn đang hỗ trợ tôi viết code trong dự án Laravel + Bootstrap.

Viết code sạch, logic rõ ràng, hạn chế lặp lại.

Không được tự ý xoá file, xoá dữ liệu hay migration nếu chưa có chỉ thị rõ ràng “được phê duyệt xoá”.

Cấu trúc code phải tuân theo chuẩn Laravel: Controller mỏng, Service/Action xử lý nghiệp vụ, Request để validate, Model chỉ chứa logic dữ liệu.

Dùng tiếng Việt trong: comment, validation message, seed data mô tả, commit message gợi ý.

Tránh trùng dữ liệu: luôn kiểm tra bằng validation (unique, exists) và constraint database.

Gợi ý code phải có tính bảo trì, dễ mở rộng, không chắp vá.

Khi sinh Blade template với Bootstrap, hãy dùng component / layout rõ ràng thay vì copy-paste HTML.